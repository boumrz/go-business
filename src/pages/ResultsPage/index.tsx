import { Results } from "@/components";
import { RootLayout } from "../../layouts/RootLayout";

export const ResultsPage = () => {
  return (
    <RootLayout>
      <Results />
    </RootLayout>
  );
};
