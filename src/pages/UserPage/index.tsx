import { User } from '@/components';
import { RootLayout } from "../../layouts/RootLayout"

export const UserPage = () => {
    return (
        <RootLayout>
            <User />
        </RootLayout>
    )
}